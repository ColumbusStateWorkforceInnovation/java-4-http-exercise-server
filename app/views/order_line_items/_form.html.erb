<%= form_with(model: [@order, order_line_item], url: user_order_order_line_items_path(@order.user.id, @order)) do |form| %>
  <% if order_line_item.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(order_line_item.errors.count, "error") %> prohibited this order_line_item from being saved:</h2>

      <ul>
        <% order_line_item.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :count, style: "display: block" %>
    <%= form.number_field :count %>
  </div>

  <div>
    <%= form.label :product_id, style: "display: block" %>
    <%= form.text_field :product_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
